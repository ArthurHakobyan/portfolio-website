---
import Image from "astro/components/Image.astro"
import frontend from '../assets/front-end.png';
import artur from '../assets/artur.png';

---

<section>
    <div 
    class="form-wrapper" >
        <form class="contact-form">
            <h2>Kontaktujte mě</h2>

            <input type="text" placeholder="Jméno" required>
            <input type="email" placeholder="E-mail" required>
            <textarea rows="4" placeholder="Řekněte Mi, Co Máte Na Mysli" required></textarea>

            <button type="submit">
              Odeslat zprávu 🚀
            </button>
        </form>
        <div class="img-wrapper">
          <Image src={frontend} alt='png image typed front end' width={350}/>
          <Image src={artur} alt='artur' width={350}/>
        </div>
    </div>
</section>

<style>

  .form-wrapper {
    display: flex;
    flex-direction: column;
    background-color: var(--color-bg-article);
  }

  .img-wrapper {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    margin: 0 auto;
    padding: 3rem;
    width: min(25%, 70%);

  }

 

    .contact-form {
    padding: 3rem 2rem;
    box-shadow: 0 5px 20px rgba(0,0,0,0.1);
    font-family: 'Futura Cyrillic' ;
    font-size: var(--fs-400);
  }

 

  .contact-form h2 {
    text-align: center;

  }

  .contact-form input,
  .contact-form textarea {
    width: 100%;
    padding: 10px;
    margin: 8px 0;
    border: 1px solid #ddd;
    font-family: 'Futura Cyrillic' ;
    font-size: var(--fs-400);
  }

  .contact-form button {
    width: 100%;
    padding: 1rem;
    margin-top: 1rem;
    background: transparent;
    color: var(--color-primary);
    border: 0.15rem solid var(--color-primary);
    cursor: pointer;
    font-family: 'Futura Cyrillic' ;
    font-size: var(--fs-400);
    font-weight: 500;

  }

  .contact-form button:hover {
    opacity: 0.85;
  }

  @media (min-width: 768px) {

    .form-wrapper {
    display: flex;
    flex-direction: row;
    padding: 5rem;
  }

  .img-wrapper {
    width: 50%;
    padding: 3rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }

  

  }


</style>